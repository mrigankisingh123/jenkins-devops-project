pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Build stage - version 2'
            }
        }

        stage('Test') {
            steps {
                echo 'Test stage running - version 2'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploy stage - NEW deployment'
            }
        }
    }
}
